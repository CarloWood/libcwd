This is a minimal example project using libcwd.

IMPORTANT: This example project only works of you already
           compiled and *installed* libcwd!

The old Autotools build files for this example have been removed with
the rest of the Autotools support. The source files are kept as compact
examples for now, but they are no longer a separately maintained build
project.
