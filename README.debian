Libcwd is included in the debian release.

If you are using debian then
you can get a default install with the command:

apt-get install libcwd-dev


In case you need a different configuration of libcwd
then you'll need to get the sources and configure and
compile it yourself of course.

